<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="TrimBrowser.NewItemPage"
		Title="New Item">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Save" Clicked="Save_Clicked" />
	</ContentPage.ToolbarItems>
	<ContentPage.Content>
		<StackLayout Spacing="20" Padding="15">
			<Label Text="Title" FontSize="Medium" />
            <Entry Placeholder="Enter record title" Text="{Binding Item.Description}" FontSize="Small" />
            <Image x:Name="PhotoImage" />
            <Button x:Name="CameraButton" Text="Take Photo" />
            <!--<Label Text="Description" FontSize="Medium" />
			<Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />-->
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
